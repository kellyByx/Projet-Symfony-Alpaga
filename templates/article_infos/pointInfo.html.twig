{% extends 'base.html.twig' %}

{% block body %}


    <!-- Services Start -->
    <div class="container-fluid bg-light pt-5">
        <div class="container py-5">
            <div class="d-flex flex-column text-center mb-5">
                <h3 class="text-secondary mb-3">Point info</h3>
                <h1 class=" m-0"><span class="text-primary">Alpagas:</span> Point d'informations</h1>
            </div>
            
    <!--Annonces/articles-->
    <div class="row pb-3">
            {% for articleInfo in articlesInfos %}
           
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="d-flex flex-column text-center bg-white mb-2 p-3 p-sm-5">

                        <img class="w-25 mx-auto d-block display-3 mb-3"src="{{ asset('assets/img/alpaga2.png') }}" alt=""> 

                    </br>

                        <h3 class="mb-3">{{ articleInfo.titre}}</h3>
                        <p>{{ articleInfo.resumer}}</p>
                        <a class="text-uppercase font-weight-bold" href="{{ path('articleInfos', {'id': articleInfo.id} ) }}">Lire la suite</a>
                    </div>
                </div>
                
           {% endfor %}
    </div>
    
                {# <h3 class="flaticon-food display-3 font-weight-normal text-secondary mb-3"></h3>
                <h3 class="flaticon-grooming display-3 font-weight-normal text-secondary mb-3"></h3>
                <h3 class="flaticon-cat display-3 font-weight-normal text-secondary mb-3"></h3>
                <h3 class="flaticon-dog display-3 font-weight-normal text-secondary mb-3"></h3>
                <h3 class="flaticon-vaccine display-3 font-weight-normal text-secondary mb-3"></h3> #}

           
        <!--pagination-->

         <div class="col-lg-12">
            <nav aria-label="Page navigation"></nav>
                <div class="pagination justify-content-center">
                    {{ knp_pagination_render(articlesInfos, 'article_infos/pagination.html.twig') }}
                </div>
            </nav>
        </div>

        </div>
    </div>
    <!-- Services End -->



{% endblock %} 